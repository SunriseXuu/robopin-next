services:
  robopin-next:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 3000:3000
    networks:
      - robopin-network

  nginx:
    image: nginx:latest
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
    ports:
      - 80:80
    depends_on:
      - robopin-next
    networks:
      - robopin-network
    restart: unless-stopped

networks:
  robopin-network:
    driver: bridge
